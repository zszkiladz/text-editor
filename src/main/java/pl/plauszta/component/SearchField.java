package pl.plauszta.component;

import javax.swing.*;
import java.awt.*;

public class SearchField extends JTextField {
    public SearchField() {
        this.setPreferredSize(new Dimension(100, 25));
        this.setName("SearchField");
    }
}
